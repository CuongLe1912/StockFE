export enum MMRequestStatusType {
    Waiting = 1,
    Process,
    Success
}

export enum MMRequestActionType {
    Registration = 1,
    Buy,
    Contact
}

export enum MMRequestType {
    Personal = 1,
    Anonymous
}