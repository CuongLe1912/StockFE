export enum MShareStatusType {
    Draft = 0,
    Published,
    Reject,
}

export enum MShareReportStatusType {
    NotYet = 0,
    Cancel,
    Reject,
}

export enum MShareCommentType {
    One = 1,
    Two,
}

export enum MShareCommentStatus {
    Hidden = 0,
    Showing,
}