export enum MCRMCallLogType {
    Unknow = 0,
    Inbound,
    Outbound,
}
export enum MCRMCallLogStatusType {
    Unknow = 0,
    Busy,
    Answered,
    Answering,
}
export enum MCRMCallManagementStatusType {
    SUCCESSFUL = 1,
    UNSUCCESSFUL = 2,
    MISSED = 3,
    BUSY = 4
}
export enum MCRMIFameSaleType {
    Sale = 1,
    SalePatner = 2
}