export enum MGBannerType {
    Image = 1,
    Video,
}

export enum MGBannerPageType {
    HomePage = 1,
}

export enum MGBannerStatusType {
    Draft = 0,
    NotUse,
    Active,
    Pending,
}

export enum MGBannerDevice {
    PC = 1,
    Mobile,
    Tablet
}