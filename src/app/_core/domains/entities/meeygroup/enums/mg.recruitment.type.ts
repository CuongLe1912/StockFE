export enum MgRecruitmentType {
    Normal = 0,
    Hot,
}

export enum MgRecruitmentStatusType {
    Pending = 0,
    Active,
    Pause,
    Expire
}

export enum MgRecruitmentSalaryUnitType {
    VND = 1,
    USD
}

export enum MgRecruitmentExperienceType {
    NOT_REQUIRE = 1,
    No,
    Yes
}
