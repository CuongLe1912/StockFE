import { TableData } from "./table.data";

export class NavigationStateData {
    id?: any;
    object?: any;
    viewer?: boolean;
    prevUrl?: string;
    className?: string;
    sessionKey?: string;
    prevData?: TableData;
}